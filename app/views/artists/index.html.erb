<h1>Artists</h1>

<% @artists.each do |artist| %>
  <ul>
    <h2><%= link_to 'artist.name artist.song_count', artist_path(artist) %></h2>
    <% artist.songs.each do |song|%>
      <li><%= link_to song.title, song_path(song) %></li>
    <% end %>
  </ul>
<% end %>

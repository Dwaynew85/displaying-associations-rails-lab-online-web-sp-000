<h1>Artists</h1>

<% @artists.each do |artist| %>
  <ul><h2><%= link_to artist.name, artist_path(artist) %></h2>
    <% artist.songs.each do |song|%>
      <li><%= link_to song.name, song_path(song) %></li>
    <% end %>
  </ul>
<% end %>
